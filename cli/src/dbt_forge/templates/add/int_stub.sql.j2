with source as (
    select * from {{ '{{' }} ref('stg_example_source__orders') {{ '}}' }}
),

final as (
    select
        order_id,
        customer_id,
        status,
        amount_usd,
        created_at
        -- Add {{ mart }}-specific enrichment columns here
    from source
)

select * from final
