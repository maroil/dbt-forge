{{ '{%' }} snapshot {{ name }} {{ '%}' }}

{{ '{{' }}
    config(
        target_schema='snapshots',
        unique_key='id',
        strategy='timestamp',
        updated_at='updated_at',
    )
{{ '}}' }}

select * from {{ '{{' }} source('{{ project_name }}', '{{ name }}') {{ '}}' }}

{{ '{%' }} endsnapshot {{ '%}' }}
